# Conversion to YAML from Properties formar report
# Warnings:
# - The yaml file had comments which are lost in the refactoring!
spring:
  application:
    name: PiggyBank-GiffCard
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/PiggyBank-GiffCard
    username: root
    password: 123nepal
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
    hibernate:
      ddl-auto: update
    show-sql: true
  messages:
    basename: message
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB
